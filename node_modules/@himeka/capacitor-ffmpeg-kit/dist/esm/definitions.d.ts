export interface FFmpegKitPluginPlugin {
    scanFile(options: {
        path: string;
        directory: string;
    }): Promise<void>;
    execute(options: {
        command: string;
    }): Promise<{
        returnCode: number;
    }>;
}
