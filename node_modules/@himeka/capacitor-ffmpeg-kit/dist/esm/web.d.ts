import { WebPlugin } from '@capacitor/core';
import type { FFmpegKitPluginPlugin } from './definitions';
export declare class FFmpegKitPluginWeb extends WebPlugin implements FFmpegKitPluginPlugin {
    scanFile(options: {
        path: string;
        directory: string;
    }): Promise<void>;
    execute(_options: {
        command: string;
    }): Promise<{
        returnCode: number;
    }>;
}
