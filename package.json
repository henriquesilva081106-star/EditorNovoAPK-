{
  "name": "editor-monstro-final",
  "version": "1.0.0",
  "description": "Editor de vídeo com IA",
  "scripts": {
    "build": "mkdir -p www && cp index.html www/ && cp -r assets www/assets || true"
  },
  "dependencies": {
    "@capacitor/core": "^5.0.0",
    "@capacitor/android": "^5.0.0",
    
    "@capacitor/filesystem": "^5.0.0",   <!-- OK -->
    <!-- REMOVIDO: "@capacitor/permissions": "^5.0.0", --> 
    "@capacitor/camera": "^5.0.0"       <!-- OK -->
  },
  "devDependencies": {
    "@capacitor/cli": "^5.0.0"
  }
}
